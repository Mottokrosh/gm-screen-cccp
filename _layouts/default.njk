---
title: Capes and Cloaks and Cowls and a Park—Digital GM Screen
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&family=Raleway:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/@trevoreyre/autocomplete-js/dist/style.css" rel="stylesheet">
    <link href="styles.css" rel="stylesheet">
    <script src="https://unpkg.com/@trevoreyre/autocomplete-js"></script>
    <title>{{ title }}</title>
    <style>
      :root {
        --sectionWidth: 280px;
        --gridGap: 1rem;
      }

      * {
        box-sizing: border-box;
      }

      *::before, *::after {
        box-sizing: inherit;
      }

      body {
        font-family: 'Raleway', sans-serif;
        margin: 0;
        padding: 2rem;
        background-image: url("img/NaturalKnit.jpg");
        background-size: 300px;
      }

      img {
        max-width: 100%;
      }

      strong {
        font-weight: 700;
      }

      h1, h2, h3, h4 {
        font-family: 'Cinzel Decorative', cursive;
        line-height: 1;
      }

      h1 {
        text-align: center;
        display: flex;
        flex-direction: column;
        color: rgb(112, 4, 85);
        text-shadow: 2px 2px 2px rgba(112, 4, 85, 0.5);
      }

      h1 span:first-of-type {
        font-size: 1.25rem;
      }

      h1 span:last-of-type {
        font-size: 2.5rem;
      }

      main {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(var(--sectionWidth, 300px), 1fr));
        grid-gap: var(--gridGap, 1rem);
      }

      section {
        border-radius: 0.25rem;
        background-color: rgba(255, 255, 255, 0.5);
        background-image: url("img/line-horiz.svg"), url("img/line-vert.svg"), url("img/line-horiz.svg"), url("img/line-vert.svg");
        background-size: 200px 8px, 8px 200px, 200px 8px, 8px 200px;
        background-position: top, right, bottom, left;
        background-repeat: repeat-x, repeat-y, repeat-x, repeat-y;
      }

      section header {
        background-color: rgb(0, 0, 0, 0.8);
        padding: 1rem;
        margin: 0.25rem;
        position: relative;
      }

      section header::after {
        content: "";
        position: absolute;
        left: 0;
        width: 100%;
        bottom: -2px;
        height: 8px;
        background-image: url("img/line-horiz.svg");
        background-size: 200px 8px;
        background-position: bottom;
        background-repeat: repeat-x;
      }

      section h2 {
        margin: 0;
        color: #fff;
      }

      ol {
        margin: 0;
      }

      .scrollable {
        padding: 1rem;
        margin-bottom: 1rem;
        max-height: 320px;
        overflow-y: auto;
      }
    </style>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
